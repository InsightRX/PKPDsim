# Transform a vector into a string that evaluates to the same vector

Collapses a vector into a comma-separated list with strings quoted (and
special characters escaped). A general purpose helper function for
writing new model code.

## Usage

``` r
vector_to_R_code(vec)
```

## Arguments

- vec:

  a vector

## Value

character string of length 1
