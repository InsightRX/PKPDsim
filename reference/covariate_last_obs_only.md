# Use only last observed covariate values

Use only last observed covariate values

## Usage

``` r
covariate_last_obs_only(covariates)
```

## Arguments

- covariates:

  covariates object

## Value

List containing same elements as input covariate object but including
only the last value for each covariate
