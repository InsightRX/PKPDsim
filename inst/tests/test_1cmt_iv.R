library(testthat)

test_that("Model tests run", {
  # This test gets called as part of the installation process
  # if run_tests = TRUE.
  # model_from_api will stop if test fails/can't be found.
  expect_true(TRUE)
})
