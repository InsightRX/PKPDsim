#' Save a model as an R library
#'
#' Save the PKPDsim model as a compiled model. Benefit is much faster load
#' times, e.g. for use in Shiny app.
#'
#' @param model PKPDsim
#' @param folder folder for new library
#' @param parameters default parameter list

model_to_library <- function() {

  return()
}
